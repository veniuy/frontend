import React from 'react';

const QuinceaneraCreativeTemplate = () => {
  return (
    <div className="quinceanera-template">
      <style jsx>{`
        @import url('https://fonts.googleapis.com/css2?family=Pacifico&family=Playfair+Display:wght@400;700&display=swap');

        :root {
          --primary-color: #E91E63;
          --secondary-color: #FCE4EC;
          --text-color: #333;
          --heading-font: 'Pacifico', cursive;
          --body-font: 'Playfair Display', serif;
        }

        .quinceanera-template {
          font-family: var(--body-font);
          color: var(--text-color);
          line-height: 1.6;
          margin: 0;
          padding: 0;
          box-sizing: border-box;
        }

        h1, h2, h3, h4, h5, h6 {
          font-family: var(--heading-font);
          color: var(--primary-color);
          margin-bottom: 0.5em;
        }

        .hero-section {
          background-color: var(--secondary-color);
          color: var(--primary-color);
          text-align: center;
          padding: 100px 20px;
          position: relative;
          overflow: hidden;
        }

        .hero-section h1 {
          font-size: 4em;
          margin-bottom: 10px;
          color: var(--primary-color);
        }

        .hero-section p {
          font-size: 1.8em;
          margin-top: 0;
          color: var(--primary-color);
        }

        .hero-section button {
          background-color: var(--primary-color);
          color: white;
          border: none;
          padding: 15px 30px;
          font-size: 1.2em;
          border-radius: 30px;
          cursor: pointer;
          transition: background-color 0.3s ease;
          font-family: var(--body-font);
          margin-top: 20px;
        }

        .hero-section button:hover {
          background-color: #C2185B;
        }

        section {
          padding: 60px 20px;
          text-align: center;
        }

        .countdown-section,
        .gifts-section,
        .rsvp-section {
          background-color: var(--secondary-color);
        }

        .celebration-section,
        .gallery-section,
        .instagram-section,
        .songs-section,
        .useful-info-section {
          background-color: white;
        }

        .footer-section {
          background-color: var(--primary-color);
          color: white;
          padding: 30px 20px;
          text-align: center;
          font-size: 0.9em;
        }

        .footer-section p {
          margin: 0;
        }

        .modal {
          display: none; /* Hidden by default */
          position: fixed; /* Stay in place */
          z-index: 1; /* Sit on top */
          left: 0;
          top: 0;
          width: 100%; /* Full width */
          height: 100%; /* Full height */
          overflow: auto; /* Enable scroll if needed */
          background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
        }

        .modal-content {
          background-color: #fefefe;
          margin: 15% auto; /* 15% from the top and centered */
          padding: 20px;
          border: 1px solid #888;
          width: 80%; /* Could be more or less, depending on screen size */
        }
      `}</style>
      {/* Hero Section */}
      <section id="hero" className="hero-section">
        <h1>Mis 15 Años</h1>
        <p>Nombre de la Quinceañera</p>
        <button>Confirmar Asistencia</button>
      </section>

      {/* Countdown Section */}
      <section id="countdown" className="countdown-section">
        <h2>Faltan...</h2>
        {/* Countdown component will go here */}
      </section>

      {/* Celebration Section */}
      <section id="celebration" className="celebration-section">
        <h2>La Celebración</h2>
        <p>Detalles del evento principal.</p>
      </section>

      {/* Gallery Section */}
      <section id="gallery" className="gallery-section">
        <h2>Galería</h2>
        {/* Image gallery component will go here */}
      </section>

      {/* Gifts Section */}
      <section id="gifts" className="gifts-section">
        <h2>Regalos</h2>
        <p>Información sobre regalos.</p>
      </section>

      {/* Instagram Section */}
      <section id="instagram" className="instagram-section">
        <h2>#Mis15Años</h2>
        {/* Instagram feed component will go here */}
      </section>

      {/* RSVP Section */}
      <section id="rsvp" className="rsvp-section">
        <h2>Confirmar Asistencia</h2>
        {/* RSVP form component will go here */}
      </section>

      {/* Songs Section */}
      <section id="songs" className="songs-section">
        <h2>Sugerir Canciones</h2>
        {/* Song request component will go here */}
      </section>

      {/* Useful Info Section */}
      <section id="useful-info" className="useful-info-section">
        <h2>Información Útil</h2>
        <p>Detalles de contacto, transporte, etc.</p>
      </section>

      {/* Footer */}
      <footer className="footer-section">
        <p>&copy; 2025 Mis 15 Años</p>
      </footer>

      {/* Modals (placeholders) */}
      <div id="modal-confirm" className="modal"></div>
      <div id="modal-gallery" className="modal"></div>
    </div>
  );
};

export default QuinceaneraCreativeTemplate;
